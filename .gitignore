*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/hot
/public/storage
# Note: `public/build` contains built Vite assets. Kept out of git in development, but we now include it for Vercel static deploys.
# Remove the comment and add `/public/build` back here if you want to ignore it again.
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

.vercel
.env*.local
