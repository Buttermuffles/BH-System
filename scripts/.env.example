# Example environment variables for Chandra OCR client
# Set `CHANDRA_ENDPOINT` to the full URL of the OCR endpoint (e.g. https://api.chandra.example.com/ocr)
CHANDRA_ENDPOINT=

# Provide either `CHANDRA_API_KEY` (Bearer) or a full `CHANDRA_AUTH_HEADER_VALUE`.
CHANDRA_API_KEY=
# Or set a custom header value directly:
CHANDRA_AUTH_HEADER_VALUE=

# Optional: change header name if your API expects a different header
CHANDRA_AUTH_HEADER=Authorization
